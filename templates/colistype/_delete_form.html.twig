<form method="post" action="{{ path('colistype_delete', {'id': colistype.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ colistype.id) }}">
    <button  class="btn btn-danger">Delete</button>
</form>
